[package]
name = "rust_http"
version = "0.1.0"
edition = "2024"
authors = ["samicpp"]

[dependencies]
tokio = { version = "1.46.1", features = ["full"] }
# async-trait = "*"
async-compression = { version = "0.4.25", features = ["gzip","tokio"] }
hpack = "0.3.0"
